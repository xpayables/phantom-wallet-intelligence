# Pinned for reproducible local runs + Streamlit Cloud deploy.
requests==2.34.2
duckdb==1.5.4
pandas==3.0.3
numpy==2.5.1
scikit-learn==1.9.0
altair==6.2.2
streamlit==1.58.0
# dune-client no longer required for extraction (we call the Dune API directly); install manually if needed.
